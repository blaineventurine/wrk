# wrk configuration — generated by 'wrk init'
#
# Placeholders ({root}, {shared}, {parent}) are expanded before hooks
# run. Always quote "{shared}" in 'run' strings — on macOS the default
# storage path contains a space.
#
# Run 'wrk link' after editing to provision shared resources.

# No project files detected. Uncomment the resources you need:
#
# resources:
#   - name: env
#     path: .env
#     create: false
#
#   - name: node
#     path: node_modules
#     fingerprint:
#       - "{root}/package.json"
#       - "{root}/yarn.lock"
#     hooks:
#       initialize:
#         - run: yarn install --frozen-lockfile
#           cwd: "{root}"
resources: []
