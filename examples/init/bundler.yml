- name: bundler
  path: vendor/bundle
  fingerprint:
    - "{root}/Gemfile"
    - "{root}/Gemfile.lock"
    - "{root}/.ruby-version"
  hooks:
    initialize:
      - run: bundle install
        cwd: "{root}"
        env:
          BUNDLE_PATH: "{shared}"
