- name: node
  path: node_modules
  # No lockfile detected — add one and include it in the fingerprint.
  fingerprint:
    - "{root}/package.json"
  hooks:
    initialize:
      - run: npm install
        cwd: "{root}"
