resources:
  - name: env
    path: ".env*"

  - name: node
    path: node_modules

    fingerprint:
      - "{root}/package.json"
      - "{root}/package-lock.json"

    hooks:
      initialize:
        - run: npm ci
          cwd: "{root}"
